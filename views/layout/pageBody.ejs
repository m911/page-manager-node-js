<main class="container">
    <div class="row">
        <div class="col-lg-12">
            <%- locals.pageContent %>

                <% if (locals.fileNames) { %>
                    <% fileNames.forEach(function(file){ %>
                        <%- include(`../${file}`); %>
                            <% }); %>
                                <% } %>
        </div>
    </div>
</main>